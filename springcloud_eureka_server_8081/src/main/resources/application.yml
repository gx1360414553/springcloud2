server:
  port: 8081

eureka:
  instance:
    hostname: www.eureka2.com
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://www.eureka1.com:8080/eureka/
